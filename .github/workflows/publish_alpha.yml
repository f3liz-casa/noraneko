# SPDX-License-Identifier: MPL-2.0

name: "(A) |α| Publish Alpha"

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      win_runtime_artifact_workflow_call:
        type: string
        required: false
        default: ""
      linux_runtime_artifact_workflow_call:
        type: string
        required: false
        default: ""

permissions:
  contents: write

run-name: "|α| Publish Alpha"
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Retrieve run IDs
        run: |
          if [ -z "${{ inputs.win_runtime_artifact_workflow_call }}" ]; then
            WINDOWS_ID=$(curl -s \
              -H "Accept: application/vnd.github+json" \
              -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
              "https://api.github.com/repos/${{ github.repository }}/actions/workflows/package.yml/runs?branch=main&status=success" \
            | jq -r '[.workflow_runs[] | select(.display_title | endswith("Windows-x86_64"))][0].id')
            echo "WINDOWS_RUN_ID=$WINDOWS_ID" >> $GITHUB_ENV
          else
            echo "WINDOWS_RUN_ID=${{ inputs.win_runtime_artifact_workflow_call }}" >> $GITHUB_ENV
          fi

          if [ -z "${{ inputs.linux_runtime_artifact_workflow_call }}" ]; then
            LINUX_ID=$(curl -s \
              -H "Accept: application/vnd.github+json" \
              -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
              "https://api.github.com/repos/${{ github.repository }}/actions/workflows/package.yml/runs?branch=main&status=success" \
            | jq -r '[.workflow_runs[] | select(.display_title | endswith("Linux-x86_64"))][0].id')
            echo "LINUX_RUN_ID=$LINUX_ID" >> $GITHUB_ENV
          else
            echo "LINUX_RUN_ID=${{ inputs.linux_runtime_artifact_workflow_call }}" >> $GITHUB_ENV
          fi

      - name: Download Windows installer
        uses: actions/download-artifact@v4
        with:
          name: noraneko-windows-x86_64-installer
          run-id: ${{ env.WINDOWS_RUN_ID }}
          path: ~/noraneko-publish/win
          github-token: ${{ github.token }}

      - name: Download Linux installer
        uses: actions/download-artifact@v4
        with:
          name: noraneko-linux-x86_64-installer
          run-id: ${{ env.LINUX_RUN_ID }}
          path: ~/noraneko-publish/linux
          github-token: ${{ github.token }}

      - name: Get version info
        run: |
          # Extract version from NMA manifest if available
          if [ -f ~/noraneko-publish/win/noraneko.nma.zip ]; then
            unzip -p ~/noraneko-publish/win/noraneko.nma.zip manifest.json | jq -r '.noranekoVersion' > /tmp/version.txt
            echo "NR_VERSION=$(cat /tmp/version.txt)" >> "$GITHUB_ENV"
          else
            echo "NR_VERSION=alpha" >> "$GITHUB_ENV"
          fi

      - name: Deploy to GitHub Releases 🚀
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ~/noraneko-publish/win/*.exe
            ~/noraneko-publish/win/*.nma.zip
            ~/noraneko-publish/linux/*.tar.xz
            ~/noraneko-publish/linux/*.deb
            ~/noraneko-publish/linux/*.nma.zip
          tag_name: "alpha"
          name: "Alpha Release"
          body: |
            Noraneko Alpha Release

            ## Downloads
            - **Windows**: Use the installer (.exe) or extract and use noraneko-winupdater
            - **Linux**: Use the tarball (.tar.xz) or deb package (.deb)

            ## NMA (Noraneko Module Archive)
            The NMA file contains the browser-features modules. For updates, use [noraneko-winupdater](https://github.com/f3liz-dev/noraneko-winupdater) or your package manager.
          draft: false
          prerelease: true
          token: ${{ github.token }}

      - name: Publish Package 🎁
        uses: actions/upload-artifact@v4
        with:
          name: noraneko-publish
          path: ~/noraneko-publish/*
